<!-- section: loop -->
# Команды по кругу

Чтобы выполнить одну команду для каждого значения списка, нужен цикл `for`:

```python
numbers = [3, 6, 9]

for number in numbers:
    print(number)
```

После `for` стоит переменная `number`. На каждом повторе в неё попадает
следующее значение списка. Слово `in` можно прочитать как «из», а двоеточие
`:` открывает повторяемый блок.

Команда внутри блока сдвинута вправо четырьмя пробелами. Такой сдвиг называется
отступом. Python повторит только команды с этим отступом. В результате появятся
три строки: `3`, `6`, `9`.

Всю конструкцию можно прочитать так: «для каждого числа из numbers покажи это
число». Python берёт значения по одному, каждый раз сохраняет очередное в
`number` и выполняет блок с отступом. Частые ошибки — забытое двоеточие после
списка или отсутствующий отступ перед командой блока.

<!-- section: exercise_01 -->
## Упражнение 1. Выведи все числа

С помощью цикла `for` выведи каждое число готового списка. В результате должны
появиться четыре строки: `2`, `4`, `6`, `8`.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: exercise_03 -->
## Упражнение 2. Исправь отступ

В готовом цикле команда `print` потеряла отступ, поэтому выполняется только один
раз и показывает лишь последнее число. Сдвинь эту строку вправо четырьмя
пробелами. Должны появиться `1`, `3` и `5`.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: addresses -->
# Цикл по адресам

Если список хранит адреса, цикл может сразу распаковывать каждый адрес:

```python
cells = [(3, 6), (7, 2)]

for x, y in cells:
    draw_deck(PLAYER, x, y, DECK_IDLE)
```

На первом повторе `x` и `y` получат координаты `(3, 6)`, на втором — `(7, 2)`.
На своём поле появятся два корабля.

<!-- section: exercise_02 -->
## Упражнение 3. Нарисуй корабли циклом

Нарисуй циклом однопалубные корабли по готовому списку адресов. Корабли должны
появиться в клетках `(2, 4)`, `(5, 4)` и `(8, 4)`.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: project -->
## Пишем игру

Сохрани показ обоих полей и счётчик через `len(ships)`. Расширь список `ships`
до десяти адресов:

- `(1, 1)`, `(3, 1)`, `(5, 1)`, `(7, 1)`, `(9, 1)`;
- `(1, 3)`, `(3, 3)`, `(5, 3)`, `(7, 3)`, `(9, 3)`.

Замени повторяющиеся вызовы `draw_deck` одним циклом `for`, который нарисует
все десять кораблей. Оба поля должны остаться на экране.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: star -->
## Задача со звёздочкой

На поле противника покажи циклом три промаха: `(2, 2)`, `(5, 5)` и `(8, 8)`.
Используй готовый список адресов и знакомую команду `show_miss`.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: recap -->
# Итоги урока

Цикл `for` повторяет блок команд для каждого значения списка. Благодаря циклу
десять кораблей рисуются одной короткой частью программы.
