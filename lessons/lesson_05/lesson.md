<!-- section: append -->
# Добавляем в список

Иногда список растёт постепенно. Пустой список выглядит так:

```python
values = []
```

Команда `append` добавляет одно значение в конец списка:

```python
values.append(value)
```

Перед точкой стоит имя списка. После точки — команда для этого списка.
Строку `values.append(6)` можно прочитать так: «в список values добавь число
6». Python находит существующий список и изменяет его: новое значение
становится последним.

Аргумент:

- `value` — значение, которое нужно добавить.

Пример:

```python
values = []
values.append(6)
values.append(9)
print(values)
```

После запуска появится `[6, 9]`. `len(values)` теперь вернёт `2`.
Если поменять две команды `append` местами, поменяется и порядок значений в
списке.

<!-- section: exercise_01 -->
## Упражнение 1. Добавь число

Добавь число `5` в готовый список. Выведи сначала весь список, затем его длину.
В результате должны появиться `[2, 5]` и `2`.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: exercise_02 -->
## Упражнение 2. Добавь адрес

Добавь адрес `(7, 4)` в готовый список клеток и выведи длину списка. В
результате должно появиться `3`.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: exercise_03 -->
## Упражнение 3. Расставь по порядку

Программа добавляет два адреса не в том порядке. Поменяй местами команды
`append`, чтобы получился список `[(1, 1), (4, 1), (7, 1)]`.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: project -->
## Пишем игру

Оставь показ обоих полей и счётчик через `len(ships)`. Замени готовый список
`ships` на пустой, а затем по очереди добавь адреса `(1, 1)`, `(3, 1)`, `(5, 1)`
и новый адрес `(7, 1)` с помощью `append`. Нарисуй четыре корабля.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: recap -->
# Итоги урока

Пустой список можно заполнять постепенно. `append` добавляет новое значение в
конец, а `len` показывает, сколько значений уже собрано.
