<!-- section: coordinates -->
# Координаты клетки

В игре уже видны оба поля. Теперь программе нужно точно указать клетку,
в которой стоит корабль. Способ задавать такие адреса называется системой
координат.

Адрес клетки состоит из двух чисел: `(x, y)`.

- `x` — номер столбца слева направо;
- `y` — номер строки сверху вниз;
- на поле 10 столбцов и 10 строк, поэтому оба числа бывают от 1 до 10.

Первая координата всегда означает `x`, а вторая — `y`.

> [!EXAMPLE]
> **Пример:** адрес `(6, 3)` означает клетку в шестом столбце и третьей строке.
> Здесь порядок чисел задаёт сначала столбец, затем строку.

<!-- section: deck_api -->
# Рисуем корабль в клетке

Теперь можно выбрать клетку для корабля. Команде `draw_deck` нужно передать
несколько аргументов. Их пишут внутри скобок по порядку и разделяют запятыми.

```python
draw_deck(board, x, y, state)
```

Аргументы:

- `board` — игровое поле. Возможные значения:
  - `PLAYER` — твоё поле;
  - `ENEMY` — поле противника;
- `x` — номер столбца;
- `y` — номер строки;
- `state` — состояние палубы. Возможные значения:
  - `DECK_IDLE` — палуба не повреждена.

> [!EXAMPLE]
> **Пример:** нарисуем палубу на поле игрока в клетке `(5, 7)`.
>
> ```python
> draw_deck(PLAYER, 5, 7, DECK_IDLE)
> ```
>
> В вызове значения идут в том же порядке, что и имена аргументов в описании:
> поле игрока, столбец 5, строка 7 и состояние целой палубы. После запуска
> в клетке `(5, 7)` появится однопалубный корабль.

<!-- section: exercise_01 -->
## Упражнение 1. Нарисуй корабль

Нарисуй однопалубный корабль на своём поле в клетке `(2, 4)`. Используй
`draw_deck` и состояние `DECK_IDLE`.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: exercise_02 -->
## Упражнение 2. Исправь адрес

Корабль должен стоять в третьем столбце и седьмой строке, но в готовом вызове
координаты перепутаны. Исправь вызов. Корабль должен появиться в клетке `(3, 7)`.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: exercise_03 -->
## Упражнение 3. Исправь строй

Два корабля должны стоять в одной строке: в клетках `(2, 6)` и `(8, 6)`.
В готовой программе в обоих вызовах перепутаны столбец и строка. Исправь
координаты, чтобы корабли встали на нужные места.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: project -->
## Пишем игру

Сохрани в игре оба поля. Добавь на своё поле первый однопалубный корабль
в клетке `(2, 2)` с помощью `draw_deck`.

После запуска на экране должны остаться оба поля, а в клетке `(2, 2)` твоего поля
должен стоять корабль.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: star -->
## Задача со звёздочкой

На своём поле уже стоят три корабля в левой половине. Твоя задача — достроить
симметричный строй, добавив для каждого корабля зеркальную пару справа.

Зеркальная пара стоит на той же строке и на таком же расстоянии от правого края.

> [!EXAMPLE]
> **Пример:** если корабль стоит в клетке `(1, 4)`, его зеркальная пара находится
> в клетке `(10, 4)`.

Начальные корабли находятся в клетках `(2, 2)`, `(3, 5)` и `(4, 8)`. Добавь
для каждого из них зеркальный корабль в правой половине поля.

После запуска на поле должны быть шесть кораблей, расположенных симметрично.
Сам вычисли три недостающих адреса. Оба поля должны остаться на экране.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: recap -->
# Итоги урока

В упражнениях ты потренировался находить клетки по координатам и вызывать
`draw_deck` с несколькими аргументами. В твоей игре теперь показаны оба поля и первый
однопалубный корабль в клетке `(2, 2)`.
