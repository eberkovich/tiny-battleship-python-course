<!-- section: api -->
# Команды игрового поля

В этом курсе ты полностью создашь игру «Морской бой» с однопалубными
кораблями. Каждый такой корабль занимает одну клетку.

Проходя уроки, ты сам напишешь всю логику игры: расставишь корабли, научишь
программу проверять попадания, добавишь ходы компьютера и определишь победителя.

Чтобы показывать поля, корабли и выстрелы, ты будешь использовать
вспомогательные команды. Начнём с ними знакомиться.

---

Чтобы твоя программа узнала о вспомогательных командах, в начале каждой
программы уже есть строка:

```python
from battleship_ui import *
```

Она подключает вспомогательные команды к твоей программе. Эту строку изменять
не нужно.

---

Первая вспомогательная команда — `show_board`. Она показывает игровое поле:

```python
show_board(board)
```

Аргумент:

- `board` — игровое поле. Возможные значения:
  - `PLAYER` — твоё поле;
  - `ENEMY` — поле противника.

Пример — показать поле противника:

```python
show_board(ENEMY)
```

После запуска появится пустое поле размером 10 на 10 клеток.

<!-- section: exercise_01 -->
## Упражнение 1. Покажи своё поле

Покажи поле игрока. Используй команду `show_board` и значение нужного поля.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: coordinates -->
# Координаты и корабли

Каждая клетка имеет адрес `(x, y)`.

- `x` — номер столбца слева направо;
- `y` — номер строки сверху вниз;
- на поле 10 столбцов и 10 строк, поэтому `x` и `y` имеют значения от 1 до 10.

Первая координата всегда означает `x`, а вторая — `y`. Поэтому клетка `(6, 3)`
находится в шестом столбце и третьей строке.

---

Теперь можно выбрать клетку для корабля. Команда `draw_deck` рисует одну
палубу корабля:

```python
draw_deck(board, x, y, state)
```

Аргументы:

- `board` — игровое поле. Возможные значения:
  - `PLAYER` — твоё поле;
  - `ENEMY` — поле противника;
- `x` — номер столбца;
- `y` — номер строки;
- `state` — состояние палубы. Возможные значения:
  - `DECK_IDLE` — палуба не повреждена.

Пример — нарисовать палубу на поле игрока в клетке `(5, 7)`:

```python
draw_deck(PLAYER, 5, 7, DECK_IDLE)
```

После запуска в клетке `(5, 7)` появится палуба корабля.

---

Команда `show_miss` показывает, что выстрел в выбранную клетку не попал в
корабль:

```python
show_miss(board, x, y)
```

Аргументы:

- `board` — игровое поле. Возможные значения:
  - `PLAYER` — твоё поле;
  - `ENEMY` — поле противника;
- `x` — номер столбца;
- `y` — номер строки.

Пример — показать промах на поле противника в клетке `(8, 6)`:

```python
show_miss(ENEMY, 8, 6)
```

После запуска в клетке `(8, 6)` появится промах.

<!-- section: exercise_02 -->
## Упражнение 2. Нарисуй однопалубный корабль

Нарисуй однопалубный корабль в клетке `(2, 4)`. Используй `draw_deck` и
состояние `DECK_IDLE`.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: exercise_03 -->
## Упражнение 3. Покажи промах

Покажи промах в клетке `(4, 2)`. Используй `show_miss`.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: project -->
## Пишем игру

Используя команду `show_board`, покажи оба игровых поля:

- поле игрока;
- поле противника.

Это начало твоей игры: в следующих уроках этот код останется и станет больше.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: star -->
## Задача со звёздочкой

На своём поле нарисуй горизонтальный корабль из трёх палуб: `(3, 5)`, `(4, 5)`,
`(5, 5)`. На поле противника покажи промах в клетке `(7, 3)`.

Используй только знакомые команды `draw_deck` и `show_miss`.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: recap -->
# Итоги урока

Теперь твоя программа умеет показывать два разных поля. Ты знаешь, как найти
любую клетку по координатам `(x, y)` и как показать корабль или промах.
