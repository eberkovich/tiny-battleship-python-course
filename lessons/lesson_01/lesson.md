<!-- section: intro -->
# Первое знакомство

В этом курсе ты полностью создашь игру «Морской бой» с однопалубными
кораблями. Каждый такой корабль занимает одну клетку.

Проходя уроки, ты сам напишешь всю логику игры: расставишь корабли, научишь
программу проверять попадания, добавишь ходы компьютера и определишь победителя.

Чтобы показывать поля, корабли и выстрелы, ты будешь использовать
вспомогательные команды. С ними мы познакомимся в следующих разделах.

<!-- section: api -->
# Как устроена команда

Чтобы твоя программа узнала о вспомогательных командах, в начале каждой
программы уже есть строка:

```python
from battleship_ui import *
```

Она подключает вспомогательные команды к твоей программе. Эту строку изменять
не нужно.

---

Вспомогательные команды, которыми ты будешь пользоваться, — это функции Python.

У каждой функции есть имя. Чтобы выполнить функцию, напиши её имя и круглые
скобки. Такая запись называется вызовом функции.

Функции встречаются в самых разных программах. Они дают действию понятное имя,
чтобы его можно было выполнить тогда, когда оно понадобится. В этой игре
первыми такими действиями будут показ поля, корабля и выстрела.

Некоторым функциям нужна дополнительная информация. Её пишут внутри скобок.
Такая информация называется аргументом.

Порядок записи — имя функции, круглые скобки и аргумент внутри — называется
синтаксисом вызова.

---

Первая вспомогательная команда — `show_board`. Она показывает игровое поле:

```python
show_board(board)
```

Аргумент:

- `board` — игровое поле. Возможные значения:
  - `PLAYER` — твоё поле;
  - `ENEMY` — поле противника.

Пример — показать поле противника:

```python
show_board(ENEMY)
```

После запуска появится пустое поле размером 10 на 10 клеток.

<!-- section: comments -->
# Комментарии в коде

В файлах с заданиями ты увидишь строки, которые начинаются со знака `#`:

```python
# Покажи поле игрока.
```

Такая строка называется комментарием. Комментарий — это подсказка для человека,
который читает код. Python не выполняет комментарии и сразу переходит к
следующей строке программы.

Пиши свой код под комментарием. Сам комментарий можно оставить в файле.

<!-- section: exercise_01 -->
## Упражнение 1. Покажи своё поле

Покажи поле игрока. Используй команду `show_board` и значение нужного поля.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: coordinates -->
# Координаты и корабли

Система координат — это способ дать каждой клетке точный адрес. Она нужна,
чтобы программа понимала, где нарисовать корабль или показать выстрел.

Адрес каждой клетки состоит из двух чисел: `(x, y)`.

- `x` — номер столбца слева направо;
- `y` — номер строки сверху вниз;
- на поле 10 столбцов и 10 строк, поэтому `x` и `y` имеют значения от 1 до 10.

Первая координата всегда означает `x`, а вторая — `y`. Поэтому клетка `(6, 3)`
находится в шестом столбце и третьей строке.

---

Теперь можно выбрать клетку для корабля. Команде `draw_deck` нужно передать
несколько аргументов. Их пишут внутри скобок по порядку и разделяют запятыми.

Команда `draw_deck` рисует одну палубу корабля:

```python
draw_deck(board, x, y, state)
```

Аргументы:

- `board` — игровое поле. Возможные значения:
  - `PLAYER` — твоё поле;
  - `ENEMY` — поле противника;
- `x` — номер столбца;
- `y` — номер строки;
- `state` — состояние палубы. Возможные значения:
  - `DECK_IDLE` — палуба не повреждена.

Пример — нарисовать палубу на поле игрока в клетке `(5, 7)`:

```python
draw_deck(PLAYER, 5, 7, DECK_IDLE)
```

После запуска в клетке `(5, 7)` появится палуба корабля.

---

Команда `show_miss` показывает, что выстрел в выбранную клетку не попал в
корабль:

```python
show_miss(board, x, y)
```

Аргументы:

- `board` — игровое поле. Возможные значения:
  - `PLAYER` — твоё поле;
  - `ENEMY` — поле противника;
- `x` — номер столбца;
- `y` — номер строки.

Пример — показать промах на поле противника в клетке `(8, 6)`:

```python
show_miss(ENEMY, 8, 6)
```

После запуска в клетке `(8, 6)` появится промах.

<!-- section: exercise_02 -->
## Упражнение 2. Нарисуй однопалубный корабль

Нарисуй однопалубный корабль в клетке `(2, 4)`. Используй `draw_deck` и
состояние `DECK_IDLE`.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: exercise_03 -->
## Упражнение 3. Покажи промах

Покажи промах в клетке `(4, 2)`. Используй `show_miss`.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: project -->
## Пишем игру

Используя команду `show_board`, покажи оба игровых поля:

- поле игрока;
- поле противника.

Это начало твоей игры: в следующих уроках этот код останется и станет больше.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: star -->
## Задача со звёздочкой

На своём поле уже стоят три однопалубных корабля в левой половине. Дострой для
каждого зеркальный корабль в правой половине: строка остаётся той же, а столбцы
образуют пары `1` и `10`, `2` и `9`, `3` и `8`, `4` и `7`, `5` и `6`.

После запуска на поле должны быть шесть кораблей, расположенных симметрично.
Сам вычисли три недостающих адреса. Оба поля должны остаться на экране.

> [!NOTE]
> Открой редактор → выполни задание → сохрани изменения → нажми **«Запустить»**.

<!-- section: recap -->
# Итоги урока

Теперь твоя программа умеет показывать два разных поля. Ты знаешь, как найти
любую клетку по координатам `(x, y)` и как показать корабль или промах.
